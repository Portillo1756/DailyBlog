// 4// target button by id
// const button = document.getElementById('incrementNum')

// // retrieve the number from LS. If there is no key for that number in LS, initialize it as 0
// // data needs to be parsed for it to be usable in JS
// let number = JSON.parse(localStorage.getItem('savedNum'))||0
// console.log(number)

// // on click callback function
// incrementAndDisplay = ()=>{
//     // increment the number
//     number++
//     // log the number in the consoel
//     console.log(number)
//     // store the number in LS. needs to be stringified before storing in LS
//     localStorage.setItem('savedNum', JSON.stringify(number))
// }

// button.addEventListener('click', incrementAndDisplay)
